export class aliments{
    id: number
    nom: string
    quantite:number

    constructor(id:number,nom: string, quantite: number,){
        this.id= id
        this.nom= nom
        this.quantite= quantite
    }
}